<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.tkrsk.cityfit_light">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_non_duplicate"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_non_duplicate_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cityfitlight">
        <activity
            android:name=".views.SimpleSettings"
            android:exported="true" />
        <activity
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>